import React from 'react';

const NotFoundPage = () => {
    return (
        <div style={{display: 'flex', justifyContent: 'center'}}>
            <h1 style={{color: 'red', fontSize: '50px'}}>Not Found Page</h1>
        </div>
    );
};

export default NotFoundPage;